include "base-config.conf"

kafka {
  input.topic = ${job.env}".hcx.request.fetch"
  audit.topic = ${job.env}".hcx.audit"
  groupId = ${job.env}"-fetch-group"
}

task {
  consumer.parallelism = 1
  downstream.operators.parallelism = 1
}