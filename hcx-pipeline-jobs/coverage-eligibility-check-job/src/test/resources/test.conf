include "base-test.conf"

kafka {
  input.topic = "local.hcx.request.eligibilitycheck"
  audit.topic = "local.hcx.audit"
  groupId = "local-coverage-eligibility-check-group"
}

task {
  downstream.operators.parallelism = 1
}